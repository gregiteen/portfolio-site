# Git Sync Strategy

The portfolio relies on a manual build and git commit strategy instead of GitHub actions.

- **Why:** To retain direct control over the `dist/site` build outputs and ensure the AI generator's logic is cleanly committed to history without automated runners modifying the repository invisibly.
